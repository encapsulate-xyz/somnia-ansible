---
# maintains testnet inventory grouped by project names
all:
  vars:
    env: testnet
  children:
    somnia:
      hosts:
        validator.somnia.testnet.encapsulate.xyz:
          type: validator
